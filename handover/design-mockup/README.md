# Design mockup — where it lives

The v2 mockup is a live, editable design canvas (not static files):

**https://claude.ai/code/artifact/cab7927c-2f50-41ac-ad48-fb8c70dafd0c**

- Two artboards: **Desktop 1440px** and **Mobile 390px**, in the existing FameNinja design language (navy #050b1d–#12295f, brand blue #2563eb, green #22c55e, Inter).
- Two sticky notes on the canvas: (1) every v2 change vs the live page, (2) the bracketed placeholders that need real values before deploy.
- Toolbar → Export gives PNG (per artboard) or a single PDF of both — export copies for offline reference if needed.
- Ankush owns the artifact; he needs to share it with you (it is private by default).

Implementation source of truth: the mockup for visuals, `HANDOVER.md` §4 for the complete change list.
